# Preview Project Workflow

Follow these steps to run the local development preview server.

## 1. Start Dev Server
```bash
node ./node_modules/next/dist/bin/next dev -p 9002
```

## 2. Access Preview
Open `http://localhost:9002` in your browser.
