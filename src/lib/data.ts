// This file is no longer the source of truth for article data.
// The data is now fetched from MongoDB via server actions in `src/app/actions.ts`.
// A seed script at `scripts/seed.ts` is used to populate the database with initial data.
export {};
